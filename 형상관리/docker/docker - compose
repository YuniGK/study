https://www.notion.so/bf3e1931cf674e5dbd8383d20080dc15?pvs=4
https://www.notion.so/d563bba6ee724de8a34c65c9fbb29e4b?pvs=4


참조
https://github.com/Around-Hub-Studio/around-hub-docker


**docker compose

https://docs.docker.com/compose/
단일 서버에서 여러개의 컨테이너를 하나의 서비스로 정의해 컨테이너의 묶음으로 관리할 수 있는 작업 환경을 제공하는 관리 도구

ㅌdocker-compose up
#docker-compose.yml 파일을 실행

docker-compose -f ${파일 이름.yml} up
#docker-compose.yml 파일이름이 다를 경우 -f를 통해서 찾는다.

docker-compose up -d
#도커 컴퍼즈를 백그라운드에서 실행

- 여러 컨테이너의 설정을 하나의 YAML 파일로 정리 및 실행 가능
- 각종 도커 오브젝트를 추가하거나 설정을 추가하는 것이 매우 편리
- 동일한 호스트 내에서 여러 컨테이너를 쉽게 관리할 수 있다는 장점
	=> 프로덕션 용으로 적합하지 않고, 개발 및 테스트 용도로 적합